function find(query) {
  const {
    name,
    extension,
    size,
    hash,
    title,
    releaseDate,
    platform,
    description,
    genres,
    players,
    coop,
    rating,
    images,
    externalIds,
    similarGames
  } = query;
}
